# Used by next.config.ts rewrite (server-side, proxies /api/* → backend)
API_BASE_URL=http://127.0.0.1:8000/api/v1

# Used by axios in browser — points to the Next.js proxy (same origin, no CORS)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3000/api